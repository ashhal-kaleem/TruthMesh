# Core dependencies
numpy
pandas
scikit-learn
tqdm
matplotlib
seaborn

# Web scraping and search tools
requests
beautifulsoup4
python-whois
# selenium removed: replaced with requests+BeautifulSoup for Render compatibility

# LLM API clients

google-genai
ollama

# LLM frameworks
langchain
langgraph
langchain-google-genai
langchain-groq
langchain-community

# Utilities and configuration
python-dotenv
pydantic
jsonlines
tenacity
pyyaml

# Academic search and citation tools
scholarly
biopython

# Additional utilities
urllib3

# API Backend
fastapi
uvicorn
python-multipart

# Database (PostgreSQL + ORM)
sqlalchemy
psycopg2-binary        # SQLAlchemy / ORM
psycopg[binary]        # pgvector backend (_PgVectorStore) — psycopg v3 handles
                       # Supabase transaction pooler (PgBouncer) correctly

# Authentication
python-jose[cryptography]
passlib[bcrypt]
bcrypt<5  # passlib 1.7.x incompatible with bcrypt 5.x

# Vector store (production — set VECTOR_BACKEND=pgvector to activate)
# Requires DATABASE_URL pointing to a PostgreSQL instance with pgvector
# (e.g. Supabase) and GOOGLE_API_KEY.
# Uses psycopg (v3) for the pooler connection; psycopg2 is retained for
# SQLAlchemy only.
