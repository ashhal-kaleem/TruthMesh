# ── Gemini / Serper API keys (required for real pipeline) ──────────────────────
GOOGLE_API_KEY=your_google_api_key_here
SERPER_API_KEY=your_serper_api_key_here

# ── Database ───────────────────────────────────────────────────────────────────
# PostgreSQL (production):
#   DATABASE_URL=postgresql://user:password@host:5432/factagent
# SQLite (local dev, no install required):
DATABASE_URL=sqlite:///./factagent.db

# ── Authentication ─────────────────────────────────────────────────────────────
# Generate a secure random string: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=CHANGE_ME_TO_A_RANDOM_64_CHAR_STRING
JWT_ALGORITHM=HS256
JWT_EXPIRE_DAYS=7

# ── Vector Store ───────────────────────────────────────────────────────────────
# "fake"     = InMemoryVectorStore + FakeEmbeddings (dev / test, no extra install)
# "chromadb" = ChromaDB + sentence-transformers (production, pip install chromadb sentence-transformers)
VECTOR_BACKEND=fake
CHROMA_DIR=./chroma_db

# ── CORS ───────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins, or * for all
CORS_ORIGINS=*

# ── Environment ────────────────────────────────────────────────────────────────
ENVIRONMENT=development